local M = {
  formatters = require("custom.configs.diagnosticls.formatters"),
  filetypes = require("custom.configs.diagnosticls.filetypes"),
  linters = require("custom.configs.diagnosticls.linters"),
}

return M
